{
  "name": "lugn-push-server",
  "version": "1.0.0",
  "description": "Web Push backend for the Lugn PWA — schedules and delivers notifications even when the app is closed.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "web-push": "^3.6.7"
  }
}
